<UserControl
    x:Class="STranslate.Plugin.Ocr.SimpleTex.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.Plugin.Ocr.SimpleTex.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ikw:SimpleStackPanel Spacing="12">
        
        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Ocr_SimpleTex_AppID}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Password_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <TextBox MinWidth="300" Text="{Binding AppId, UpdateSourceTrigger=PropertyChanged}" />
        </ui:SettingsCard>

        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Ocr_SimpleTex_AppSecret}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Password_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <TextBox MinWidth="300" Text="{Binding AppSecret, UpdateSourceTrigger=PropertyChanged}" />
        </ui:SettingsCard>

        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Ocr_SimpleTex_Website}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Globe_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ui:HyperlinkButton NavigateUri="https://simpletex.cn/">
                <TextBlock Text="https://simpletex.cn/" />
            </ui:HyperlinkButton>
        </ui:SettingsCard>

    </ikw:SimpleStackPanel>
</UserControl>